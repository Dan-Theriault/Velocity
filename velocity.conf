[Defaults]
# if editor is not specified, velocity will use the global defaults
# xdg-open recommended on linux, open on MacOS, start on windows
editor = xdg-open
book = Notes

[Notes]
bookpath = /home/dtheriault3/Notes
extension = .md
editor = gnome-terminal
arg1 = -e
# ' /absolute/path/to/your/file' is appneded to last argX
arg2 = vim

[Philosophy]
bookpath = /home/dtheriault3/Documents/PHIL-108
extension = .xoj
editor = xournal
# templates are stored in ~/.velocity/Templates
template = template.xoj
